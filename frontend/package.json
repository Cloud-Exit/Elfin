{
  "name": "lefin-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun build src/app.ts --outdir ../static --watch",
    "build": "bun build src/app.ts --outdir ../static --minify",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@arrow-js/core": "^1.0.0-alpha.11"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
